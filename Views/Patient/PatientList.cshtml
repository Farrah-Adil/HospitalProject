@model IEnumerable<HospitalProject.Models.PatientEntity>
@{
    ViewData["Title"] = "PatientList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>PatientList</h1>
<hr />

<p class="float-left">
    <a asp-action="AddPatient" asp-controller="Patient" class="btn btn-outline-secondary">Add Patient</a>
</p>

<p class="float-right">
    @* <a asp-action="AddCustomer" asp-controller="Item" class="btn btn-outline-secondary">Add School</a> |*@
    @*<a asp-action="List" class="btn btn-outline-primary">Item List</a> |
    <a asp-action="SchoolList" asp-controller="Item" class="btn btn-outline-primary">Category List</a>*@
</p>
<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Age)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Gender)
            </th>
          @*  <th>
                @Html.DisplayNameFor(model => model.HosId)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.DepId)
            </th>*@

            <th>
                @Html.DisplayNameFor(model => model.HosName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DepName)
            </th>


            <th>Actions</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Age)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Address)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Gender)
                </td>

                @*<td>
                    @Html.DisplayFor(modelItem => item.HosId)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.DepId)
                </td>*@

                <td>
                    @Html.DisplayFor(modelItem => item.HosName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DepName)
                </td>



                <td>
                    @Html.ActionLink("Update", "AddPatient", new {item.PtId, item .Name,item.Age, item.Address,item.Gender,item.HosId,item.DepId } , new { @class="btn btn-outline-success rounded-pill"}) |
                    @Html.ActionLink("Delete", "DeletePatient", new {  item.PtId  }, new { @class = "btn btn-outline-danger rounded-pill",onclick="return confirm('Do you really want this record?')" })
                </td>

            </tr>
        }
    </tbody>
    </table>
